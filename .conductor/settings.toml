"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "bun install"
run_mode = "concurrent"

[scripts.run.dev]
command = "bun run dev --port $CONDUCTOR_PORT"
default = true
icon = "play"

[scripts.run.storybook]
command = "bun run storybook -p $CONDUCTOR_PORT"
icon = "book-open"
